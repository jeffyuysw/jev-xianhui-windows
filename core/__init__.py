"""Platform-independent judgment kernel.

Nothing in this package touches the OS, the GUI, or the capture layer. It is the
same question set and the same scoring rules as the Android build, so both
platforms triage messages identically.

Ported from 先回 (Android) - itself a second development of
https://github.com/jev-chat/jev-chat-jarvis
"""

__version__ = "1.0.3"
