# 更新日志

## v1.0.5

**修复（三个都影响实际使用）**

- **分析记录窗口不会自动刷新** —— 窗口开着时收到新消息、判断完成后列表不更新，
  得关掉重开才看得到。现在把它注册为 store 监听器：窗口可见时自动刷新，并
  **保持滚动位置**（否则你正翻旧记录时来条新消息，列表会突然跳回顶部）
- **提示条可能被裁剪** —— `_resize_to_content` 用 `isVisible()` 判断提示条是否存在，
  但 Qt 里父窗口还没 `show()` 时子控件的 `isVisible()` 也是 `False`，于是
  「点开始监听的瞬间就报找不到窗口」这一时序下，提示条高度被算成 0、窗口显示后
  又不会重算。改用 `isHidden()` 后正常（实测 79 → 109）
- **退出时不关闭记录库连接** —— 现在 `quit()` 里显式 `close()`

**清理**

- 删除未引用代码：`store.pending_judge()`、`store.latest_text()`、`window.client_origin()`
- 删除未使用 import：`ACCENT`、`DEFAULT_ENDPOINT`、`DEFAULT_MODEL`

## v1.0.4

**新增：分析记录**

- 设置窗口标题最右边新增「分析记录」入口
- 顶部可按状态筛选：要马上回 / 尽快 / 可以晚点 / 判断中 / 失败 / 全部，
  **默认只看「马上回」**
- 每行显示：来源（微信 / QQ）、名称（联系人）、聊天内容、时间
- 最多保留 **2000 条**，一页 **25 条**，滚到底自动续加载下一页
- 数据存在本机 `%APPDATA%\JevPriority\history.db`（SQLite），不联网、不上传，
  页面内可一键清空
- 判断失败的消息也会留痕，记录页里能看出「这条来过、只是没判成功」

## v1.0.3

**悬浮窗**
- **保留最近 5 条**，同一个人连发的多条分别显示。早先只显示最新 1 条，而且
  同一个发送者的第二条消息会把第一条覆盖掉
- **显示全部状态**：要马上回 / 尽快 / 可以晚点 / 判断中 都留在列表里。
  早先会过滤掉「可以晚点」，用户反馈那样会漏消息
- **显示对方昵称**：单聊时从会话标题取名字，不再一律显示「对方」
- **点击即跳转**：点某条消息直接切到对应的聊天应用，自动识别是微信还是 QQ
  （另外还认钉钉 / 飞书 / TIM / 企业微信），并把这条从列表移除
- **提示条**：微信窗口没打开时，标题下方显示
  「微信窗口需要打开（请不要最小化和关闭）」

**新增**
- 设置窗口底部新增**官网入口**，点击用系统默认浏览器打开
- 设置窗口新增**开机自动启动**开关（写 HKCU 的 Run 项，不需要管理员权限、
  不弹 UAC）

**修复**
- **跨线程 UI 更新失效**：点「测试连接」只显示「正在测试…」、结果永远不出现，
  采集线程的状态也刷新不到界面。原因是 `QTimer.singleShot` 要求调用线程自己
  有 Qt 事件循环，而采集线程与测试线程都是普通 `threading.Thread`，定时器
  永远不会触发。改用跨线程信号（Qt 自动排队到接收者所在线程）
- 版本号与 CHANGELOG 不同步：CHANGELOG 早已写到 1.0.1，但三处版本号还是
  1.0.0，界面上一直显示旧版本

**已知限制**
- Windows 没有系统级消息通知接口，只能截取微信窗口做本地文字识别，因此
  **微信窗口必须处于可见（非最小化）状态**才能读到新消息。最小化后窗口没有
  渲染画面，截图结果为空白。悬浮窗会就此给出提示。这是 Windows 截图机制的
  固有约束，非本程序缺陷

## v1.0.1

**悬浮窗外观**
- 悬浮窗**高度改为随消息数量伸缩**：没有消息时只有 40 px 的细条（原来是固定 110 px 的空白卡片），
  有消息时逐条长高，封顶 340 px 后改为卡片内滚动
- 新增 **不透明度** 1–100%，滑杆拖动实时生效，默认 100%（实心）
- 新增 背景色 / 文字色 / 字号 / 宽度 自定义，RGB 自选，默认白底黑字
- 悬浮窗标题与内容改为紧贴，不再留空隙
- 悬浮窗默认宽度收窄到 220 px

**修复**
- 修复悬浮窗卡死、新消息不显示：采集线程直接操作 Qt 控件，改为编组回 GUI 线程
- 修复 `EnumWindows` 遍历无响应窗口时阻塞主线程（改用 `SendMessageTimeoutW` + 800 ms 缓存）
- 修复变化检测用全图均值、位移不触发的问题（改用 24×24 分块结构签名）
- 修复托盘图标在每次消息通知时被重复创建（数千个泄漏图标导致界面冻结）
- 修复设置页「测试连接」在工作线程中直接 setText

**消息读取修正**
- 修复悬浮窗把**左侧会话列表**当成消息的问题（曾出现「折叠置顶聊天」、群名被当作发信人）。
  改为先从截图里定位会话列表与聊天窗格之间的分界线，只保留窗格内的文字
- 修复聊天输入框的**占位提示**（如「按住鼠标语音输入文字」）被当成一条消息
- 修复窗口定位会误选**浏览器标签页**的问题：原来按标题含「微信」取最大窗口，
  一个标题里带「微信」的网页标签（窗口更大）会把微信挤掉。改为按进程名 `weixin.exe` 精确定位，
  标题匹配仅作兜底

**已知限制**
- Windows 没有系统级消息通知接口，只能截取微信窗口做本地文字识别，因此
  **微信窗口必须处于可见（非最小化）状态**才能读到新消息。最小化后窗口没有渲染画面，
  截图结果为空白。这是 Windows 截图机制的固有约束，非本程序缺陷

## v1.0.0

首个 Windows 版本，产品名 **先回**。

**命名**
- 产品名从「消息优先级助手」改为「先回」（英文 XianHui）
- 安装包 `XianHui-Setup.exe`，程序 `XianHui.exe`
- 配置文件目录保留 `%APPDATA%\JevPriority`，避免已保存的密钥失效

**判断内核（core/）**
- 由 Jev 判断模型驱动（TypeSafe System One，非大语言模型）
- 与 Android 版同一套四道判断题：紧急度（1–9）/ 要马上回 / 类型 / 为什么急
- 同一套分档规则：要马上回或 ≥7 分 → 红；≥4 分 → 黄；<4 分 → 灰
- 唯一网络出口是一个 JSON POST，消息原文不落盘

**采集层（capture/）**
- 用 user32 `PrintWindow` 截取微信窗口（不截全屏），位图只在内存
- RapidOCR（ONNX）本地离线中文识别，图片不上传任何云
- 解析器按左右气泡与行间距提取最新一条对方消息，不依赖固定像素布局
- 帧指纹跳过未变化的画面，静止时几乎零开销

**界面层（ui/）**
- 可拖动悬浮窗，松手自动吸附到最近的屏幕边缘，可折叠成计数条
- 红 / 黄 / 灰 / 蓝四档配色，计数角标，整行可点即移除
- 主界面三张状态卡 + 状态总览 + 实时队列计数
- 系统托盘常驻，关主窗不退出

**打包**
- PyInstaller onedir 免安装目录 + Inno Setup 6 安装向导
- 安装包含开始菜单、桌面快捷方式与卸载项

**来源**
- 复用 [jev-chat-jarvis](https://github.com/jev-chat/jev-chat-jarvis) 的判断契约（`noul / choice / score`）
- 采集思路借鉴 [jev-chat-windows](https://github.com/jev-chat/jev-chat-windows)
- 删除生成层，只保留判断，是纯判断型工具
